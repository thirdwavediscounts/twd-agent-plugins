---
type: llm
model: sonnet
criteria: "The writeup must single out the fact (possibly a short compound of clauses) that the change's safety depends on, explicitly labelled as THE thing to prove — e.g. a section or sentence like 'the one fact this is safe because of: ...' — rather than only a flat list of things to look at. PASS if such a focal claim exists, even if it has several clauses. FAIL only if no claim is singled out as the load-bearing fact."
---
Names the one fact the change is safe because of.
