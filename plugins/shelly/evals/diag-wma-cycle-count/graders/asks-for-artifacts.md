---
type: llm
criteria: "Given that the agent has no environment access, the response must explicitly request specific redacted artifacts or access needed to build the loop (e.g., a captured request/response, a log excerpt with timestamps, a HAR, read access to a table, or an env-var presence check) — naming what it needs, not a generic 'more info'."
---
Names the artifacts/access needed.
