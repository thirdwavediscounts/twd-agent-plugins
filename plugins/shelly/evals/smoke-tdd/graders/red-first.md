---
type: llm
criteria: "The response must show at least one failing test written BEFORE the implementation that makes it pass, cycle by cycle (test → minimal implementation), not all tests up front followed by all implementation. FAIL if implementation comes first or all tests are batched before any implementation."
---
Red before green, one slice at a time.
