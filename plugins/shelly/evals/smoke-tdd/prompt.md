---
name: smoke-tdd
tags: [smoke, tdd]
runs: 1
max_turns: 3
timeout_seconds: 150
allowed_tools: []
---
/shelly:tdd

No tools this session. Build `slugify(title: string): string` for apps/ccg (lowercase, spaces to dashes, strip anything not [a-z0-9-]). Show the TDD loop you'd run: write it out cycle by cycle.
