---
type: llm
criteria: "Before (or instead of) stating a root cause, the response must propose at least one CONCRETE, executable feedback loop that would go red on this specific bug — a failing test at a named seam, a curl/HTTP replay against a named endpoint, a replayed captured artifact, a headless-browser script, or a harness. Vague advice like 'add logging' or 'check the code' does not count. PASS only if a concrete loop is named."
---
A concrete feedback loop comes first.
