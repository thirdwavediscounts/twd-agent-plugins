schema_version: "1.1"
name: vercel-rebuild-scope
baseline_prompt:
  vercel: "A PR to our Vercel-deployed pnpm monorepo changes these files. For each, say which Vercel projects rebuild after merge and whether the PR should merge as-is."
