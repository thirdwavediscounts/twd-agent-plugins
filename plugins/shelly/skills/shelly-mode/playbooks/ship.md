### Ship

"Ship it", "push and merge" → `/shelly:ship`. Green is not safe: `/interrogate` or at least `/shelly:blast-radius` before a shared-surface merge. After landing: `/shelly:worklog`; then offer `/shelly:reflect` if the task held a correction, a hard-won recipe, or a wrong playbook step.
