### Babysit a PR

"Check on PR N", "get it green", "anything outstanding" → `/babysit-pr`. Drives conflicts, review threads, and CI to merge-ready. Merging is `/shelly:ship`, which waits for the user's go.
