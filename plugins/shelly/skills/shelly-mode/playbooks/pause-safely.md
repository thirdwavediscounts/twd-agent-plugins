### Pause safely

1. `/shelly:pause-safely`: commit or stash with a named WIP, write the resume note (state, next step, open questions) where the next session will find it (ticket comment or scratchpad path in the reply).
2. Confirm nothing is left half-written in a shared surface (Linear, PR, prod).
