### Feature

1. `/how` over the affected subsystem.
2. `/architect` for parallel design exploration; skipping is `architect skipped: <reason>`, never silent.
3. Throughput checkpoint, four items, each kept with `n/a: <reason>` when it doesn't apply: blocking first steps; independent workstreams; shared mutable state (split the target before serializing); smallest safe decomposition.
4. Delegate implementation with file paths, the named data shape, and success criteria; when multiple valid shapes exist, `/arena` instead. Review the diff yourself.
5. Verify on the matching surface; the owning app's gates (`typecheck`/`test`/`build`); `/shelly:verify-work` proves the claimed behavior blind.
6. Small ordered commits.
7. Contested design → `/interrogate` before shipping; touches `packages/*`, a migration/RPC/trigger, root config, or a frozen contract → `/shelly:blast-radius`.
8. PR; the user's go before push. Reply: what you built, what you chose and why, open decisions, tables for alternatives.
