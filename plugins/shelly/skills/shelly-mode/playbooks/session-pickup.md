### Session pickup

1. `/shelly:session-pickup` on the branch, worktree, or transcript named.
2. Re-derive the state from artifacts (git log, diff, tests, ticket), not the prior session's summary.
3. Continue as Feature / Bug fix from the first unfinished step.
