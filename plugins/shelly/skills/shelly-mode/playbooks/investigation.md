### Investigation

Read-only: how does X work, why was Y built this way, are we sure, X or Y? → `/how` (Explain or Critique mode); motivation questions add `/why`; a cross-system symptom with an unknown cause → `/investigate`, which also posts the plan to Linear. No PR. If the answer precedes a code change, hand back and re-route. Reply: the `/how` shape or a recommendation with a tradeoffs table, with your real judgment.
