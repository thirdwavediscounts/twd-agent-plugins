### Plan something big

The user drives these (they're user-invoked): suggest `/shelly:grill-with-docs` (or `/shelly:wayfinder` when questions keep chaining) → `/shelly:to-spec` → `/shelly:to-tickets`. shelly-mode's part is the `/how` + `/why` groundwork before the interview and the throughput checkpoint after tickets exist.
