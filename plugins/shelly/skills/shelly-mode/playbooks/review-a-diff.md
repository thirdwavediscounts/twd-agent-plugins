### Review a diff

1. Scope: the diff or files named; on a branch, `git diff main...HEAD`.
2. `/shelly:interrogate` (standards reviewer + fresh-context Fable + security auditor + one Opus 4.8 reviewer; lead judgment stays here).
3. Touches a shared surface → `/shelly:blast-radius`.
4. Review-bot comments on the PR: fix real ones, dismiss noise with a concrete reason, ask on the ambiguous.
5. Reply: Act on / Consider / Noted / Dismissed with `path:line` and a fix per Act-on item. No edits unless asked.
